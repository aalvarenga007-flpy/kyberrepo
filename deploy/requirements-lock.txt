# Dependencias fijadas a las versiones del entorno Windows que FUNCIONA
# (venv Python 3.14.6, relevado 27/08/2026).
#
# En Rocky el venv se crea con Python 3.12 de AppStream, asi que pip va a
# resolver las transitivas para esa version. Por eso se fijan las directas y
# no el freeze completo: un freeze de 3.14/Windows tiene ruedas que en 3.12/Linux
# no existen y la instalacion falla.
#
# DESPUES de instalar en Rocky: correr 'pip freeze > requirements-rocky.lock'
# y versionar ESE archivo. Ese es el lock real del servidor.

streamlit==1.60.0
pandas==3.0.5
SQLAlchemy==2.0.51
PyMySQL==1.2.0
python-dotenv==1.2.2
openpyxl==3.1.5
anthropic==0.120.2

# FALTABA en el requirements.txt original. Lo importa presupuestador.py en las
# lineas 329 y 567, con un import perezoso y SIN try/except: no falla al
# arrancar, falla recien cuando alguien sube un PDF al presupuestador.
pdfplumber==0.11.10
